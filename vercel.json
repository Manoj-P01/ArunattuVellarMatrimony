{
  "version": 2,
  "builds": [
    {
      "src": "backend/package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "cd ../frontend && npm install && npm run build && mkdir -p ../backend/public && cp -r dist/. ../backend/public/ && cd ../backend && npm install",
        "buildCommand": "next build"
      }
    }
  ]
}
