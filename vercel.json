{
  "buildCommand": "cd frontend && npm install && npm run build && mkdir -p ../backend/public && cp -r dist/* ../backend/public/ && cd ../backend && npm install && npm run build",
  "outputDirectory": "backend/.next",
  "installCommand": "echo 'Install handled in buildCommand'",
  "framework": "nextjs"
}
