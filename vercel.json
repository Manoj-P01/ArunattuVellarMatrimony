{
  "framework": "nextjs",
  "rootDirectory": "backend",
  "installCommand": "npm install --prefix ../frontend && npm install",
  "buildCommand": "npm run build --prefix ../frontend && mkdir -p public && cp -r ../frontend/dist/. public/ && next build",
  "outputDirectory": ".next"
}